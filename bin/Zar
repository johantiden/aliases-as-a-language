#!/usr/bin/env bash

source "$(dirname $0)/../impl.sh"
__debugecho ": (sudo)sublime all repo(s)"

count=$(__johan_aliases_all_repos | wc -l)

if ask "Count is $count, continue?" Y; then
  __johan_aliases_all_repos | xargs z
fi;