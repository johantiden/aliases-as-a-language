#!/usr/bin/env bash

source "$(dirname $0)/../impl.sh"
__debugecho ": (sudo)sublime all file(s)~"

count=$(__johan_aliases_list_all_files_from_home | wc -l)

if ask "Count is $count, continue?" Y; then
  __johan_aliases_list_all_files_from_home | xargs z
fi;