#!/usr/bin/env bash

source "$(dirname $0)/../impl.sh"
__debugecho ": remove one exact (auto)repo(s)"

target=$(_impl_all_repos | __impl_one_exact);
if ask "'rm $target', continue?" N; then
  #rm $target
  echo DRYRUN: rm $target
fi;