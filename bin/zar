#!/usr/bin/env bash

source "$(dirname $0)/../impl.sh"
__debugecho ": sublime "${@}" all repo(s)"

count=$(_impl_all_repos | wc -l)

if ask "Count is $count, continue?" Y; then
  _impl_all_repos | xargs z
fi;