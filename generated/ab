#!/usr/bin/env bash

source "$(dirname $0)/../impl.sh"
__debugecho ":  all branch(es)"

count=$(__aaal_get_all_branches | wc -l)

if ask "Count is $count, continue?" Y; then
  __aaal_get_all_branches
fi;